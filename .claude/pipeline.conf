# .claude/pipeline.conf — branch-role configuration (ADR-0089 D1).
# key=value, one per line. "#" starts a whole-line comment. Parsed by the
# sole parser, tools/pipeline_config.py. An absent file resolves to the
# same values this file holds: develop/main.
integration_branch=develop
release_branch=main
